# Base Classes

Inside the `dashboard/utilities/base_classes` directory the base classes for page as `iam-base-page.php` and section as `iam-base-section.php` exist.

These 2 classes hold the main logic and functionality that register the proper hooks and do the right actions etc. The base classes are abstract classes so that they can be extended upon with separate page/section files.

## [IAM_Base_Page](./IAM_Base_Page.md)

## [IAM_Base_Section](./IAM_Base_Section.md)
